import 'package:flutter/material.dart';

class MealOverviewScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Container(
      child: new Text("Meal Overview"),
    );
  }
}
